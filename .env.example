# Database Configuration
DATABASE_URL="postgresql://username:password@host:port/database?schema=public"

# Server Configuration
PORT=3001
NODE_ENV=production
CORS_ORIGIN="https://your-frontend-domain.vercel.app"

# JWT Configuration
JWT_SECRET="your-super-secret-jwt-key-here"
JWT_EXPIRES_IN="24h"
REFRESH_TOKEN_EXPIRES_IN="7d"

# Deployment Notes:
# 1. Replace DATABASE_URL with your Supabase connection string
# 2. Update CORS_ORIGIN with your deployed frontend URL
# 3. Generate a secure JWT_SECRET for production
# 4. These variables need to be set in Vercel dashboard